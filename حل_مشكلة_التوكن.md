# 🔧 حل مشكلة "التوكن غير صحيح"

إذا ظهر لك `التوكن غير صحيح` في لوحة التحكم أو في GitHub Actions، اتبع هذا الدليل:

---

## 1️⃣ كيف تنسخ التوكن بشكل صحيح؟

### الخطأ الشائع:
كثير ناس ينسخون مع التوكن كلام إضافي!

**مثال خاطئ:**
```
Use this token to access the HTTP API:
1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
Keep your token secure and store it safely, it can be used by anyone to control your bot.
```

**الصحيح:**
انسخ فقط هذا الجزء:
```
1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
```

### الطريقة الصحيحة من @BotFather:

1. افتح تليجرام → ابحث عن `@BotFather`
2. اكتب `/mybots`
3. اختر بوتك (مثلاً @Trading_TOB_bot أو أي اسم اخترته)
4. اضغط `API Token`
5. سيظهر لك التوكن - اضغط عليه لنسخه تلقائياً (بدون مسافات)
6. الصقه مباشرة!

---

## 2️⃣ تأكد أن التوكن جديد ولم يلغى:

إذا أعطيت التوكن لأحد أو نشرته بالغلط، BotFather يلغيه!

**الحل:**
1. في @BotFather → `/mybots` → اختر بوتك → `API Token` → `Revoke current token`
2. سيعطيك توكن جديد
3. استخدم الجديد

---

## 3️⃣ اختبر التوكن يدوياً:

افتح هذا الرابط في متصفح جوالك (استبدل TOKEN بتوكنك):

```
https://api.telegram.org/botTOKEN/getMe
```

مثال:
```
https://api.telegram.org/bot1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw/getMe
```

- إذا ظهر `{"ok":true, "result":{"username":"..."}}` → التوكن صحيح ✅
- إذا ظهر `{"ok":false, "error_code":401, "description":"Unauthorized"}` → التوكن غلط ❌

---

## 4️⃣ تأكد أن البوت مشرف في القناة:

حتى لو التوكن صحيح، إذا البوت ليس مشرف، النشر يفشل!

**الحل:**
1. افتح قناتك @Trading_TOB
2. اضغط على اسم القناة فوق → `Edit` (تعديل) → `Administrators` (المشرفين)
3. `Add Admin` → ابحث عن يوزر البوت (مثلاً @Trading_TOB_bot)
4. أضفه وفعّل صلاحية `Post messages`

---

## 5️⃣ حل مشكلة المسافات:

أحياناً عند النسخ من الجوال، تنضاف مسافة في البداية أو النهاية!

**الحل:**
- قبل ما تلصق التوكن، تأكد ما فيه مسافة
- في لوحة التحكم، أنا أعمل `strip()` تلقائياً يحذف المسافات، لكن في GitHub Secrets لازم تتأكد بنفسك

---

## 6️⃣ جرب من GitHub Actions:

1. اذهب لمستودعك → Actions → `📱 نشر يدوي من الجوال`
2. اختر `test - اختبار التوكن فقط`
3. Run workflow
4. اضغط على التشغيل وشوف Logs
5. سيقول لك بالضبط إذا التوكن صحيح واسم البوت

---

## ✅ التوكن الصحيح شكله:

- يبدأ بأرقام: `1234567890:`
- بعده حروف وأرقام طويلة: `AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw`
- الطول حوالي 45 حرف
- يحتوي على `:` في النص

**مثال توكن صحيح (وهمي):**
```
1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
```

---

## 🆘 إذا جربت كل شيء وما اشتغل:

1. ارسل لي أول 10 أحرف من التوكن فقط (لا ترسل كامل!)
   مثال: `1234567890:AAH...`
2. ارسل لي صورة للخطأ
3. سأساعدك!

**لا ترسل التوكن كامل في أي مكان عام!** التوكن مثل كلمة السر، أي أحد معه التوكن يقدر يتحكم في بوتك!
