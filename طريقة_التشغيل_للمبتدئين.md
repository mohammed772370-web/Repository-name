# 🎯 طريقة التشغيل للمبتدئين - خطوة بخطوة بالصور

أنت قلت البوت عندك مربوط بالقناة ويرد فقط على /start
هذا لأنك أنشأت البوت من @BotFather لكن ما شغلت الكود الخاص بنا

سأعطيك **3 طرق** من الأسهل للأصعب، اختر ما يناسبك:

---

## 🟢 الطريقة 1: تشغيل على جهازك (أسهل - 5 دقائق)

### للويندوز:

**الخطوة 1: ثبت بايثون**
1. ادخل https://www.python.org/downloads/
2. اضغط Download Python
3. شغل الملف وثبت مع تفعيل ✅ Add python to PATH

**الخطوة 2: حمل مشروع البوت**
1. ادخل https://github.com/mohammed772370-web/Repository-name
2. اضغط Code → Download ZIP
3. فك الضغط في مجلد مثل `C:\TradingBot`

**الخطوة 3: شغل البوت**
1. ادخل المجلد `C:\TradingBot`
2. اضغط مرتين على ملف `تشغيل.bat`
3. سيظهر لك:
   ```
   هل هذه أول مرة تشغل البوت؟
   1 = نعم، أريد الإعداد
   2 = لا، شغل البوت مباشرة
   ```
4. اختر `1`
5. سيطلب منك:
   - **توكن البوت**: الصق التوكن اللي عندك
   - **معرف القناة**: مثلاً `@YourChannel`
   - **ايديك**: ابحث عن `@userinfobot` في تليجرام وأرسل له رسالة، سيعطيك رقم، انسخه
   - **مفتاح AI**: اضغط Enter للتخطي (يعمل بدون AI)

6. سيقول `✅ تم إنشاء ملف .env`
7. سيقول `هل تريد تشغيل البوت الآن؟` اكتب `y`

**مبروك!** 🎉 البوت يعمل الآن
- اذهب لتليجرام واكتب `/start` للبوت
- جرب `/education` و `/news`
- جرب `/post_education` لنشر درس للقناة

**مهم:** لا تغلق النافذة السوداء، البوت يجب أن يبقى مفتوح!

---

### للموبايل (أندرويد):
1. ثبت تطبيق Pydroid 3 من متجر جوجل
2. افتحه → انسخ ملفات البوت
3. شغل `إعداد_البوت.py` ثم `main.py`

---

## 🟡 الطريقة 2: تشغيل 24 ساعة مجاناً (بدون ما يبقى جهازك شغال)

### على Render.com (مجاني):

1. ادخل https://render.com وسجل حساب مجاني
2. اضغط New + → Background Worker
3. اربط حساب GitHub واختر مشروع `Repository-name`
4. في الإعدادات:
   - **Name**: trading-bot
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
5. اضغط Advanced → Add Environment Variable وأضف:
   ```
   BOT_TOKEN = التوكن حقك
   CHANNEL_ID = @قناتك
   ADMIN_IDS = ايديك
   TIMEZONE = Asia/Riyadh
   ```
6. اضغط Create
7. البوت سيعمل 24 ساعة مجاناً! ✅

### على Replit.com (أسهل):

1. ادخل https://replit.com
2. اضغط Create → Import from GitHub
3. الصق رابط مشروعك
4. في Secrets (القفل على اليسار) أضف:
   - `BOT_TOKEN`
   - `CHANNEL_ID`
5. اضغط Run

---

## 🔴 الطريقة 3: سيرفر VPS (للمحترفين)

إذا عندك سيرفر:

```bash
git clone https://github.com/mohammed772370-web/Repository-name.git
cd Repository-name
pip install -r requirements.txt
python إعداد_البوت.py
# ثم
nohup python main.py &
```

---

## ❓ أسئلة سريعة:

**س: البوت مربوط بالقناة لكن ما ينشر؟**
ج: لأنك ما شغلت الكود، البوت الحالي من BotFather فارغ، لازم تشغل `main.py` عندنا

**س: كيف أجيب معرف القناة؟**
- لو قناتك عامة: مثلاً رابطها `t.me/MyChannel` → المعرف `@MyChannel`
- لو خاصة: حول رسالة من القناة لبوت `@userinfobot` → سيعطيك `-1001234567890`

**س: كيف أجيب ايدي حسابي؟**
- ابحث عن `@userinfobot` في تليجرام
- أرسل له أي رسالة
- سيرد: `Your ID: 123456789`

**س: هل لازم ذكاء اصطناعي؟**
لا! البوت فيه قوالب احترافية جاهزة، يعمل بدون AI. الـ AI فقط يحسن الصياغة

**س: البوت يشتغل لكن ما ينشر تلقائياً؟**
تأكد:
1. البوت مشرف في القناة مع صلاحية النشر
2. CHANNEL_ID صحيح
3. الوقت بتوقيت الرياض Asia/Riyadh

**س: أريد تغيير أوقات النشر؟**
افتح ملف `.env` وعدل:
```
EDUCATION_POST_TIMES=10:00,18:00
NEWS_POST_TIMES=08:00,14:00,20:00
```

---

## 🆘 إذا واجهت مشكلة:

1. تأكد أنك ثبت بايثون وفعّلت Add to PATH
2. تأكد أن التوكن صحيح (جرب ترسله لـ @BotFather واكتب /mybots)
3. تأكد أن البوت مشرف في القناة
4. شغل `python test_bot.py` لاختبار بدون تليجرام
5. راسلني وارسل صورة للخطأ

---

## 🎬 فيديو شرح (قريباً)

إذا ما فهمت الكتابة، أقدر أعمل لك فيديو شرح أو أساعدك خطوة بخطوة

**الآن:**
1. حمل المشروع ZIP
2. شغل `تشغيل.bat`
3. اتبع التعليمات

5 دقائق والبوت شغال! 🚀
